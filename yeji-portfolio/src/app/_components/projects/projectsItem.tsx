import Image from "next/image";
import { ReactNode } from "react";

export default function ProjectItem({
    key,
    data,
}: {
    key: any;
    data: ReactNode;
}) {
    return (
        <>
            <h1>d</h1>
        </>
    );
}
